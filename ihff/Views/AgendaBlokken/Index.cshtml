@using ihff.Models
@Model ActivitiesPerLocation

<h1>Blokken schema</h1>


<div id="wishlist-wrapper">
    <table id="wishlist-table">
        <thead>
            
        </thead>
        <tbody>
    <tr>
        <th>Pathe zaal 1</th>
        @foreach (var activity in Model.ActivityPatheZaal1)
        {
            //link naar detailpagina film
            <td>
                @activity.name <br />
                @activity.type <br />
                @activity.startTime.TimeOfDay <br />
                @activity.endTime.TimeOfDay <br />
                @activity.location <br />
                @Html.ActionLink("Add to wishlist", "AddItem", new { Id = activity.Id });
            </td>

        }
    </tr>
            <tr>
                <th>Pathe zaal 9</th>
                @foreach (var activity in Model.ActivityPatheZaal9)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>
            <tr>
                <th>Pathe zaal 13</th>
                @foreach (var activity in Model.ActivityPatheZaal13)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>

            <tr>
                <th>Philharmonie Van Beinum zaal</th>
                @foreach (var activity in Model.philharmonieVanBeinumZaal)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>

            <tr>
                <th>Philharmonie Artiestenfoyer</th>
                @foreach (var activity in Model.PhilharmonieArtiestenfoyer)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>

            <tr>
                <th>Toneelschuur Filmzaal 1</th>
                @foreach (var activity in Model.toneelschuurFilmzaal1)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>

            <tr>
                <th>Toneelschuur Filmzaal 2</th>
                @foreach (var activity in Model.toneelschuurFilmzaal2)
                {
                    //link naar detailpagina film
                    <td>
                        @activity.name <br />
                        @activity.type <br />
                        @activity.startTime.TimeOfDay <br />
                        @activity.endTime.TimeOfDay <br />
                        @activity.location <br />
                    </td>
                }
            </tr>


</tbody>
    </table>
</div>




